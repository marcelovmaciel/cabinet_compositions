# v5 refinement and provisional primary completion

All seven focused evidence workers were integrated. Their HANDOFF.md, merge files and retained sources are in the worker directories. canonical_input_changes.csv records source-table changes relative to the verified v4 baseline; parent_code.patch records builder/report/validation changes. The release itself contains all final canonical inputs, including primary_assumptions.csv.

31 new evidence records strengthen the targeted history. Marinho and Damares departure bounds are narrower; Sachsida has an exact-person historical registry match; Maria Estella capacity is corrected. Other source findings remain explicit unresolved historical facts. The parent corrected date-alternative scope in the residual export.

The user's later instruction explicitly requires every primary date to be filled even when evidence remains unknown. Twelve separately flagged assumptions select no additional party contribution for 100 unique dates (105 overlapping person-days) across nine people. Original affiliation states remain UNKNOWN. The primary export has 4,096 dates; historical completeness remains 3,996/4,096. No party witness is fabricated.

The package verifier checks package hashes, exact v5 reconstruction and all tests, then exact 47-file v4 reconstruction using preserved inputs. Historical --require-complete continues to fail where evidence is unresolved; --require-primary-coverage enforces full primary coverage. Source research, factual resolution and provisional imputation must not be conflated.
